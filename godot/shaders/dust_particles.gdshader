shader_type particles;

// Note: This shader is not currently used
// Dust particles use ParticleProcessMaterial instead
// This file is kept for future reference if custom particle shaders are needed

// Minimal valid shader (required by Godot)
void vertex() {
	// Empty - particles are handled by ParticleProcessMaterial
}
